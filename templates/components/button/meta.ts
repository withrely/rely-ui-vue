import type { ComponentMeta } from '../../../types';

const meta: ComponentMeta = {
  name: 'button',
  files: ['components/button/Button.vue', 'components/button/index.ts'],
  dependencies: {
    core: ['primitive'],
  },
};

export default meta;
