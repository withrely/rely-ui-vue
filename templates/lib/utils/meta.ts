import type { ComponentMeta } from '../../../types';

export default {
  type: 'components:lib',
  dependencies: {
    external: ['clsx', 'tailwind-merge'],
    registry: [],
  },
} satisfies ComponentMeta;
